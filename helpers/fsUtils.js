// *todo filler functions to be written later.
const readFromFile = (file) =>{
	console.info(`${file}`)
}

const readAndAppend = (file) =>{
	console.info(`${file}`)
}

module.exports = { readAndAppend, readFromFile }